import React from 'react';
const Home = () => {
	return (
		<div className='home'>
			Welcome to the hostel manager
			<button>Student</button>
			<button>Manager</button>
		</div>
	);
};

export default Home;
